STREETSYNAPSE_ENV=development
STREETSYNAPSE_API_HOST=127.0.0.1
STREETSYNAPSE_API_PORT=5000

# Reference values only: uvicorn host/port are selected by the launch command.
# Bus-tracker configuration is separate: bus-tracker/.env.example.

