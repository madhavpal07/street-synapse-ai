# Web dashboard

One React application will initially contain two routes:

- `/authority` — incident map, evidence, filters, and ticket status
- `/public` — current buses, stops, routes, last update, and ETA

Use the sample JSON in `docs/api-contract.md` until the backend is available. The map must never invent live information; display a clear last-updated time.

