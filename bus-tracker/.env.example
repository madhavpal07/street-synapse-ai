# Run npm run setup to generate your own keys in .env.
# These placeholders are intentionally invalid. Do not use them as keys.
PORT=3000
HOST=0.0.0.0
DASHBOARD_KEY=REPLACE_ME
DRIVER_KEYS_JSON={"BUS-001":"REPLACE_ME","BUS-002":"REPLACE_ME"}

# Optional: development only, exact current tunnel hostname, no https:// or path
# DEV_TUNNEL_HOST=your-current-host.trycloudflare.com
